{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Comelit Intercom",
        "data": {
          "host": "IP Address",
          "port": "Port",
          "dedicated_user": "Dedicated user name or email (recommended)",
          "token": "Token (optional)"
        },
        "description": "Enter the IP address of your Comelit intercom. Recommended for local (cloud-free) doorbell events: in the device web UI (port 8080, users page) create a user for Home Assistant and pair it once in the Comelit app, then enter its name or email here so Home Assistant uses its own identity. Otherwise leave both empty to auto-extract the wall-monitor token using the default 'comelit' password.",
        "data_description": {
          "port": "ICONA Bridge port (default 64100 - leave as-is unless you use port forwarding)",
          "dedicated_user": "Name or email of a user you created and paired via the app; its token is read from a device backup",
          "token": "Advanced: use an existing user token directly"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the device",
      "invalid_auth": "Invalid authentication token",
      "auto_token_failed": "Failed to extract token automatically. Please check that the device is accessible and using the default 'comelit' password, or enter your token manually.",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Comelit Intercom options",
        "data": {
          "enable_notifications": "Doorbell ring notifications (cloud push)",
          "push_token": "Push identity token (optional)"
        },
        "data_description": {
          "enable_notifications": "Receive real-time ring events via Comelit's cloud (FCM). Requires the device to have internet access.",
          "push_token": "Advanced: enroll push under a different ICONA token so a paired phone keeps its own notifications. Leave empty to use the main token."
        }
      }
    }
  }
}